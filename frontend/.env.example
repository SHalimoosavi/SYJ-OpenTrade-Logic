# Point this at your running FastAPI server (server_fastapi/main.py).
# On Termux, if you run both the API and this dashboard on the same device,
# localhost works fine. If you access the dashboard from a different device
# on the same network, use your Termux device's LAN IP instead.
VITE_API_BASE_URL=http://localhost:8000
